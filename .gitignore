8e11301 (feat(i18n): SSR metadata with locale cookie parsing; client dynamic head for CSR updates\n\n- fix(layout): remove client directive; add generateMetadata with cookie header parsing\n- chore(fonts): add self-hosted Geist @font-face and public/fonts structure\n- fix(ui): place use client at top in features component\n- i18n: language selector labels are self-referential (English/中文)\n)
